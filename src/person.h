#pragma once
#include <vector>
#include <string>

namespace oop_practice2 {
    class Person {
    public:
        std::string name;
        int age;
        std::vector<std::string> hobbies;
    };
}